# Application
spring.application.name=portfolio-analyzer
server.port=8081

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/portfolio_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=your-secret-key-change-this-in-production-min-256-bits-long-at-least
jwt.expiration=86400000

# Finnhub API
finnhub.api.key=d5u4k6hr01qtjet20al0d5u4k6hr01qtjet20alg

# CORS
cors.allowed.origins=http://localhost:3000,https://portfolio-risk-analyzer-seven.vercel.app

# Logging
logging.level.com.portfolio.analyzer=DEBUG
logging.level.org.springframework.security=DEBUG
